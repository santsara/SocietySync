package com.example.sosy.repository;

import com.example.sosy.model.FestivalBudget;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FestivalBudgetRepository extends JpaRepository<FestivalBudget, Long> {
}